<UserControl x:Class="AnimeXdcc.Wpf.Download.DownloadEpisodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.05*" />
            <ColumnDefinition Width="0.90*" />
            <ColumnDefinition Width="0.05*" />
        </Grid.ColumnDefinitions>
        <Grid Row="0" Column="1">
            <TextBlock Text="Downloads"
                       HorizontalAlignment="Center"
                       FontSize="30"
                       FontFamily="{StaticResource HeaderFontFamily}"
                       Margin="20"/>
        </Grid>
        <Grid Row="1" Column="1">
            <StackPanel>
                <TextBlock Text="{Binding Package.FileName}"
                           FontSize="15"
                           FontFamily="{StaticResource HeaderFontFamily}" 
                           Margin="10"/>
            </StackPanel>
        </Grid>
        <Grid Row="2" Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Row="0">
                <ProgressBar Value="{Binding DownloadProgress.PercentageComplete}" />
            </Grid>
            <Grid Row="1" HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding DownloadProgressHumanReadable.TransferedBytes}"/>
                    <TextBlock Text=" of " />
                    <TextBlock Text="{Binding DownloadProgressHumanReadable.TotalBytes}"/>
                    <TextBlock Text=" (" />
                    <TextBlock Text="{Binding DownloadProgressHumanReadable.PercentageComplete}" />
                    <TextBlock Text=") [" />
                    <TextBlock Text="{Binding DownloadProgressHumanReadable.TransferSpeed}" />
                    <TextBlock Text="]" />
                </StackPanel>
            </Grid>
            <Grid Row="2">
                <TextBlock></TextBlock>
            </Grid>
        </Grid>
    </Grid>
</UserControl>